" Colorizes text
colo moriarty
syntax on

" Diplays line number
set nu

" Autoindent code depending on file extension
filetype indent on

" Replaces tab with spaces (super useful for Python)
set expandtab
set tabstop=4
set softtabstop=4
set shiftwidth=4

" Kind of wraps text
set textwidth=80
